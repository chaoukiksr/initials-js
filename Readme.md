# Initials-js

A simple javascript package to get initials from a name, an array of name, a user object, an array of user objects.
Associate color code for each result.

## Install
```bash
npm install initials-js
```

